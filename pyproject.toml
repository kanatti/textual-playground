[project]
name = "textual-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "textual>=3.1.1",
]

[dependency-groups]
dev = [
    "textual-dev>=1.7.0",
]
